import { darkColors, lightColors } from './colors'
import { fontSize, screenDimensions, space } from './spaces'

const light = { colors: lightColors, screenDimensions, fontSize, space }

const dark = { colors: darkColors, screenDimensions, fontSize, space }

export { light, dark }
